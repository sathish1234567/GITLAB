youtube.com/watch?v=akD-DpolilE
Setup repo:
__________
1. GIT INIT 
2. GIT CLONE
3. GIT CONFIG
4. GIT ALIAS


Save changes:
_____________
1. GIT ADD
2. GIT COMMIT
3. GIT DIFF
4. GIT STASH

Inspect Repo:
_____________
1. GIT status
2. git log
3. git tag
4. git blame.

Paraller development:
_____________________
1. Git Branch
2. GIt Merge
3. GIT REBASE

Git init: _______
1.  basically careates a new and empty git repository .
2.  it can be used to convert an existing /unversioned project to a git repository.
3.  this is the first command, you will run in a new project.
4.  used for initialize the repository.
5.  if you will use it in existing repository, it will reinitializing the repository to exist directory.

git clone: ------------
1.  which is used to target an existing repository and create a clone or copy of the target repository on your local meachine
git clone <repo> <directory>
2. copy the repository from git lab, use below command
open your directory in command line c:/>edureka
c:/> git clone "https://...edurekatestgit/edurekarep.git"  --> press enter 
now git has been clones to the local direcotry.
3. now change the direcory to edurekarep
 cd edurekarep/
 ls --> disply the files in current directory.

